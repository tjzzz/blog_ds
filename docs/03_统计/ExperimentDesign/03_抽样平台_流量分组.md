---
update_time: "2026-09-13 17:41:21 CST"
tags:
  - statistics/experiment-design
  - experiment/traffic
---

# 03_抽样平台_流量分组

抽样平台负责把实验方案落到稳定、随机、可追踪、可复现的线上流量分组上。它是实验可信度的基础设施。

一句话：**抽样平台要保证实验组和对照组除了实验处理不同，其他方面尽量随机一致**。

## 1. 抽样平台要解决的问题

整合 [抽样技术](../抽样技术/抽样技术.md) 与 [实验流量分层与正交设计](实验流量分层与正交设计.md)，线上实验流量平台本质是一个：

```text
稳定分桶 + 流量治理 + 配置下发 + 命中记录 + 质量监控
```

它要解决：

- 同一个用户每次访问都稳定命中同一实验版本；
- 多个实验可以长期并行；
- 互斥实验不能同时作用到同一用户；
- 正交实验可以复用流量；
- 可以按人群、场景、端、地域、版本、时间圈选；
- 实验上线后能追踪用户命中了哪个实验、哪个版本、哪个参数；
- 支持空转、小流量、扩量、暂停、回滚、全量。

## 2. 抽样基础

### 2.1 抽样方式

| 类型 | 方法 | 说明 |
|---|---|---|
| 非随机抽样 | 随意抽样、判断抽样、定额抽样、捕获-标记-再捕获 | 适合调查或特殊总体，不适合强因果实验 |
| 随机抽样 | 简单随机、分层抽样、系统抽样、整群抽样、多阶段抽样 | 实验设计优先使用，支撑因果推断 |

线上 AB 的核心是随机抽样 / 随机分桶；如果人群异质性很强，可先分层再随机。

### 2.2 Hash 流量切分

线上小流量实验常把流量切成很多份，例如 0~9999 共 10000 个桶：

```text
bucket = hash(layer_id + salt + unit_id) % 10000
```

如果 hash 足够随机，要抽 1% 流量，只需要选择 100 个桶。

## 3. 分桶键怎么选

分桶键决定实验单元和用户体验稳定性。

| Hash key | 适用场景 | 特点 |
|---|---|---|
| user_id | 登录用户实验 | 用户稳定命中，适合体验和转化实验 |
| device_id / cookie_id | 未登录用户实验 | 设备稳定，但清缓存/换设备会变化 |
| merchant_id / shop_id | 商家策略实验 | 商家维度稳定，适合 B 端权益和策略 |
| query | 搜索 query 维度实验 | 同一 query 稳定，但同一用户不同 query 体验可能不同 |
| qid / request_id | 请求级随机 | 每次请求可能不同，不适合稳定体验实验 |

原则：**分桶键要和实验单元一致**。

## 4. 核心抽象：域、层、桶、实验、版本

| 抽象 | 含义 | 例子 |
|---|---|---|
| 实验域 Domain | 相对独立的业务流量空间 | 首页、搜索、推荐、交易、营销 |
| 流量层 Layer | 域内一层完整流量；不同层可正交 | 搜索排序层、UI 层、广告策略层 |
| 桶 Bucket | 流量层内最小分配单位 | 10000 个桶，每桶约 0.01% |
| 实验 Experiment | 占用部分桶的策略验证单元 | 搜索召回实验、下单页文案实验 |
| 版本 Variant | 对照组和实验组 | control、treatment_1、treatment_2 |

典型链路：

```text
用户请求
  → 识别实验域
  → 判断是否满足受众条件
  → 进入一个或多个流量层
  → 每层独立 hash 分桶
  → 命中实验和版本
  → 返回实验参数
  → 记录曝光日志
```

## 5. 正交与互斥

| 关系 | 平台实现 | 适用场景 |
|---|---|---|
| 同层互斥 | 一个 Layer 内，桶只能被一个实验占用 | 两个搜索排序实验、两个价格策略实验 |
| 跨层正交 | 用户进入每层时重新 hash | UI 实验和推荐策略实验并行 |
| 父子实验 | 子实验只在父实验某个版本内运行 | 策略包内部再迭代、黑白盒实验 |
| 隔离域 | 某类实验独占一个业务域或人群 | 高风险策略、核心交易链路、长期 holdout |

判断是否互斥：

- 是否同时改变同一个用户体验？
- 是否共同影响同一个核心指标，导致归因不清？
- 一个实验结果是否依赖另一个实验版本？
- 是否存在工程或策略依赖？

如果答案是“会”，优先同层互斥；如果弱相关，可以跨层正交并在分析中检查交互。

## 6. 线上抽样平台模块

| 模块 | 关键能力 | 设计要点 |
|---|---|---|
| 实验配置中心 | 创建实验、配置版本、分配流量 | 配置版本化，支持审批和回滚 |
| 分流引擎 | 实时判断用户命中哪个版本 | 高并发、低延迟、稳定 hash |
| 受众圈选 | 按属性、行为、端、地域、版本过滤 | 圈选条件必须在各组一致可观测 |
| 流量层管理 | 管理域、层、桶、占用关系 | 可视化冲突、剩余流量、互斥关系 |
| 参数下发 | 返回实验参数或策略开关 | 参数有默认值，失败回退 control |
| 曝光日志 | 记录实际命中和曝光 | 分析应基于实际曝光/触发 |
| 监控告警 | 检查流量、指标、数据质量 | SRM、分桶均匀性、护栏异常 |
| 分析报表 | 输出实验效果 | 指标、涨跌幅、置信区间、分层分析 |

## 7. 关键数据表

| 表 | 关键字段 |
|---|---|
| experiment_config | experiment_id、domain、layer、status、owner、start_time、end_time、traffic_ratio |
| variant_config | experiment_id、variant_id、variant_name、is_control、traffic_ratio、params |
| layer_bucket_map | domain、layer_id、bucket_id、experiment_id、occupied_status |
| audience_rule | experiment_id、rule_type、rule_config、version |
| exposure_log | unit_id、experiment_id、variant_id、bucket_id、request_id、exposure_time、scene |
| metric_dataset | experiment_id、unit_id、variant_id、metric_name、metric_value、date |
| experiment_decision | experiment_id、decision、reason、risk、operator、decision_time |

## 8. 平台质量检查

上线前：

- 实验是否选对 domain 和 layer；
- 同层是否有流量冲突；
- 受众条件是否会导致两组不可比；
- 指标口径和数据集是否确认；
- control 默认参数是否可回退。

运行中：

- 流量是否按配置比例进入；
- 是否出现 SRM；
- 错误率、延迟、崩溃率是否异常；
- 曝光日志和业务日志是否能 join；
- 护栏指标是否异常。

分析前：

- 先看 SRM 和数据完整性；
- 再看核心指标；
- 最后看分层、长期效果和业务解释。

## 9. 整合来源

- [抽样技术](../抽样技术/抽样技术.md)：抽样类型、hash 流量切分、hash key 选择、流量分层思想。
- [实验流量分层与正交设计](实验流量分层与正交设计.md)：域、层、桶、实验、版本、正交互斥、平台模块和表设计。

下一步进入：[04_实验指标设计](04_实验指标设计.md)。
