> 2013年谷歌提出Word2vec，简单的说就是将word转成vector


概述
• 利用数据集：大型语料库——大量自然语言句子的集合
• 模型目的：预测句子出现的概率
• 主要知识点：链式法则，大数定理

# 语言模型-问题描述
假设一个句子 W = (w1, w2,....wT)， 由T个词按照顺序组成，则句子的概率模型就是w1,w2,...wt的联合概率,即:
$$P(w) = P(w_1, w_2, ...w_t) = p(w_1)p(w_2|w_1)p(w_3|w1,w2)...$$

但是实际操作的时候，计算每个的条件概率，这个参数空间太多了。


具体计算的时候有如下几种方式：
- n-gram
- 决策树
- 条件随机场
- 神经网络

## n-gram模型

(1) 从统计模型的角度看：
考虑$p(w_k|w_1,..w_{k-1})$的近似计算，根据贝叶斯公式以及大数定律有：
$$p=p(w1,...w_k)/p(w_1,...w_{k-1})≈ count(w_1,...w_k) / count(w_1,...w_{k-1})$$

容易知道，如果k很大时候，要计算这个共线串的count，会比较耗时。

n-gram的一个假定就是，一个词只与他前面的n-1个词有关，从而做了简化。每次通过滑动窗口，提取长度为n的词组作为一个训练样本。

实际应用中，n一般取3，即认为只与前面两个词有关，每次滑动窗口取长度是3的字符串。

细节处理：
用count的方式来近似概率，会存在一个天然的问题。比如count(w1,w2, ...wk)=0，并不一定能认为count(w1,w2...w(k-1))=0. 在边界处容易存在因为样本中出现次数较低导致的不置信问题。

(2) 另外一种角度
在机器学习领域有一种通用的解决问题的方法：对所考虑的问题建模后先为其构造一个目标函数，然后对这个目标函数进行优化，从而求得一组最优的参数，最后利用这组最优参数对应的模型来进行预测。
对于上面的问题，利用最大对数似然，可以将目标函数设置为
$$log(\Pi p(w|context(x))) = \sum log F(w, context(w);\theta)$$
这样有了函数F后，不用保存原先的概率值，而直接用模型进行预测即可



## CBOW

## skip-gram



# 训练过程
#todo

以skip-gram方式为例，word2vec的目标函数如下
$$\sum_{t=1}^T \sum_{-c<=j<= c} log p(w_{t+j}|w_t)$$
下面的问题就是如何定义概率$p(w_{t+j}|w_t)$ , 因为最终是要给出每个词的概率，是一个多分类问题，所以比较直接的方式就是使用softmax函数。word2vec中是这样定义的
$$p(w_0|w_1) = \frac{exp^{V_{w0}^TV_{w1}}}{\sum exp...}$$
即用两个向量的内积表示其近似程度


## 负采样


## Hierarchical softmax


# 参考

https://zhuanlan.zhihu.com/p/32829048


word2vec https://zhuanlan.zhihu.com/p/114538417




#todo word2vec工具使用