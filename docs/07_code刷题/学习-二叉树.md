## 树的遍历

主要知识点：
- 二叉树
- 二叉树的几种主要遍历方式


前中后是按照根节点来说的。 一般使用栈结构
### 前序遍历
前序遍历首先访问根节点，然后遍历左子树，最后遍历右子树。

```python
# Definition for a binary tree node.
# class TreeNode(object):
# def __init__(self, val=0, left=None, right=None):
# self.val = val
# self.left = left
# self.right = right

class Solution(object):
	
	def preorderTraversal(self, root):

		## method1: 递归
		res = [] # 全局的
		def preorder(root):
			if not root:
				return
			res.append(root.val)
			preordef(root.left)
			preordef(root.right)
		
		preordef(root)
		return res
		
	  
	def preorderTraversal2(self, root):
		## method2: 迭代
		if not root: return
		
		res = list()
		stack, node = [], root
		while stack or node:
			while node:
				res.append(node.val)
				stack.append(node)
				node = node.left
			node = stack.pop()	
			node = node.right
		
		return res

```



![](https://pic.leetcode-cn.com/Figures/binary_tree/preorder_traversal/Slide19.png)

### 中序遍历

中序遍历是先遍历左子树，然后访问根节点，然后遍历右子树。

```python
class Solution(object):
	
	def inorderTraversal(self, root):

		## method1: 递归
		res = [] # 全局的
		def preorder(root):
			if not root:
				return
			inordef(root.left)
			res.append(root.val)
			inordef(root.right)
		
		preordef(root)
		return res
		
	  
	def inorderTraversal2(self, root):
		## method2: 迭代
		if not root: 
			return
		res = list()
		stack, node = [], root
		while stack or node:
			while node:
				stack.append(node)
				node = node.left
			#
			node = stack.pop()
			res.append(node.val)
			node = node.right
		
		return res

```



![](https://pic.leetcode-cn.com/Figures/binary_tree/inorder_traversal/Slide22.png)

### 后序遍历

后序遍历是先遍历左子树，然后遍历右子树，最后访问树的根节点。
https://leetcode.cn/problems/binary-tree-postorder-traversal/solution/er-cha-shu-de-hou-xu-bian-li-by-leetcode-solution/


官网code
```python
def postorderTraversal(self, root):
	
	if not root:
		return list()
	
	res = list()
	stack = list()
	prev = None
	
	while root or stack:
		while root:
			stack.append(root)
			root = root.left
		
		root = stack.pop()
		if not root.right or root.right == prev:
			res.append(root.val)
			prev = root
			root = None
		else:
			stack.append(root)
			root = root.right
	return res

```


我们一起来看后序遍历的动画演示：![](https://pic.leetcode-cn.com/Figures/binary_tree/postorder_traversal/Slide19.png)



### 层序遍历
即按照层级依次输出。

```python
def levelOrder(self, root):
	if not root:
		return []

	que = [root]  # 队列
	res = []
	while que:
		n = len(que)
		tmp_res = []
		for i in range(n):
			node = que.pop(0)
			tmp_res.append(node.val)
			# 同时把左右节点入队列
			if node.left:
				que.append(node.left)
			if node.right:
				que.append(node.right)
		res.append(tmp_res)
	return res
```




## 删除
删除树的节点时候，按照后续遍历，即先删除左右节点，最后删除节点本身


递归与迭代

## 用递归法解决-树相关的问题
了解递归并利用递归解决问题并不容易。

当遇到树问题时，请先思考一下两个问题：

1. 你能确定一些参数，从该节点自身解决出发寻找答案吗？
2. 你可以使用这些参数和节点本身的值来决定什么应该是传递给它子节点的参数吗？

如果答案都是肯定的，那么请尝试使用 “自顶向下” 的递归来解决此问题。
或者你可以这样思考：对于树中的任意一个节点，如果你知道它子节点的答案，你能计算出该节点的答案吗？ 如果答案是肯定的，那么 “自底向上” 的递归可能是一个不错的解决方法


比如常见的问题：
- 二叉树的最大深度： max_len()


### 对称二叉树

```python
class Solution(object):
    def isSymmetric(self, root):

        def my(left, right):

            # 末节点
            if not left and not right: # 都是null
                return 1
            if not left and right or (not right and left):
                return 0

            if left and right:
                if left.val == right.val:
                    return my(left.left, right.right) * my(left.right, right.left)
                else:
                    return 0
        return my(root.left, root.right)
```

### 路径总和

![image.png](https://pic.leetcode-cn.com/1610518279-OPfMLJ-image.png)

```python
class Solution(object):

    #tmp_sum = 0

    def hasPathSum(self, root, targetSum):

        """

        :type root: TreeNode

        :type targetSum: int

        :rtype: bool

        """

        if not root:

            return 0

        if root.left is None and root.right is None: # 叶节点

            if root.val == targetSum:

                return 1

            return 0

        #print(root, targetSum)

        flag1 = self.hasPathSum(root.left, targetSum - root.val)

        flag2 = self.hasPathSum(root.right, targetSum - root.val)

        return flag1 or flag2
        
```